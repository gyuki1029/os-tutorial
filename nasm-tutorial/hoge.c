#include <stdio.h>

int main(int argc, char **argv) {
    char *test = "This is a debug demo.";
    char *name = "edo";
    int age = 20;

    printf("Name: %s, Age: %d\n", name, age);

    return 0;
}